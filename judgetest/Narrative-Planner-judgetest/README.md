embedding_test.py: uses hunyuan-embedding model to retrieve vector embedding for 2 paragraphs, and compare to judge correlation with cosine-similarity.

llm_test.py: directly uses llm-as-judge method to compare two paragraphs(using hunyuan-lite model).
